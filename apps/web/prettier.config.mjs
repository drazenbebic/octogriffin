/** @type {import("prettier").Config} */
const prettierConfig = {
  printWidth: 80,
  endOfLine: 'auto',
  arrowParens: 'avoid',
  trailingComma: 'all',
  semi: true,
  singleQuote: true,
  useTabs: false,
  bracketSpacing: true,
};

export default prettierConfig;
